@model IEnumerable<SoilMuseum.Models.Zila>

@{
    ViewBag.Title = "Zilalist";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<h4>Zila List</h4>

<p>
    @Html.ActionLink("Create New", "Zila")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Zila_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Division.Division_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Zila_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Division.Division_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "ZilaUpdate", new { id=item.Zila_ID }) 
            @*@Html.ActionLink("Details", "Details", new { id=item.Zila_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Zila_ID })*@
        </td>
    </tr>
}

</table>
